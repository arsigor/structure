{% extends '::base.html.twig' %}

{% block style %}
    {{ parent() }}
{% endblock %}

{% block header %}
{% endblock %}

{% block body %}

        <div class="container">
            <div class="row">
                <div>
                    {% if post != null %}
                        <div class="text-justify">{{ post.title }}</div>
                    {% endif %}
                    <div class="text-justify">{{ error }}</div>
                </div>
            </div>
        </div>

{% endblock %}

{% block script %}
    {{ parent() }}
{% endblock %}
